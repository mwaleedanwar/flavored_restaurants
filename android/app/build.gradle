def localProperties = new Properties()
def localPropertiesFile = rootProject.file('local.properties')
if (localPropertiesFile.exists()) {
    localPropertiesFile.withReader('UTF-8') { reader ->
        localProperties.load(reader)
    }
}

def flutterRoot = localProperties.getProperty('flutter.sdk')
if (flutterRoot == null) {
    throw new GradleException("Flutter SDK not found. Define location with flutter.sdk in the local.properties file.")
}

def flutterVersionCode = localProperties.getProperty('flutter.versionCode')
if (flutterVersionCode == null) {
    flutterVersionCode = '1'
}

def flutterVersionName = localProperties.getProperty('flutter.versionName')
if (flutterVersionName == null) {
    flutterVersionName = '1.0'
}

apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'
apply from: "$flutterRoot/packages/flutter_tools/gradle/flutter.gradle"
apply plugin: 'com.google.gms.google-services'

android {
    signingConfigs {
        configNopal {
            keyAlias 'nopal'
            keyPassword '123456' //change password to your key password
            storeFile file('./src/nopal/upload-keystore.jks')
            storePassword '123456' //change password to your key password
        }
        configMaa {
            keyAlias 'maa'
            keyPassword '123456' //change password to your key password
            storeFile file('./src/maa/upload-keystore.jks')
            storePassword '123456' //change password to your key password
        }
        configSanto {
            keyAlias 'maa'
            keyPassword '123456' //change password to your key password
            storeFile file('./src/maa/upload-keystore.jks')
            storePassword '123456' //change password to your key password
        }
        configHcb {
            keyAlias 'hcb'
            keyPassword '123456' //change password to your key password
            storeFile file('./src/hcb/upload-keystore.jks')
            storePassword '123456' //change password to your key password
        }
        configApnaa {
            keyAlias 'apnaa'
            keyPassword '123456' //change password to your key password
            storeFile file('./src/apnaa/upload-keystore.jks')
            storePassword '123456' //change password to your key password
        }
        configWillyb {
            keyAlias 'willyb'
            keyPassword '123456' //change password to your key password
            storeFile file('./src/willyb/upload-keystore.jks')
            storePassword '123456' //change password to your key password
        }
        configChaatHouse {
            keyAlias 'chaatHouse'
            keyPassword '123456' //change password to your key password
            storeFile file('./src/chaatHouse/upload-keystore.jks')
            storePassword '123456' //change password to your key password
        }
        configKabsah {
            keyAlias 'kabsah'
            keyPassword '123456' //change password to your key password
            storeFile file('./src/kabsah/upload-keystore.jks')
            storePassword '123456' //change password to your key password
        }
        configFifthE {
            keyAlias 'fifthE'
            keyPassword '123456' //change password to your key password
            storeFile file('./src/fifthE/upload-keystore.jks')
            storePassword '123456' //change password to your key password
        }
        configAlladinGrill {
            keyAlias 'alladinGrill'
            keyPassword '123456' //change password to your key password
            storeFile file('./src/alladinGrill/upload-keystore.jks')
            storePassword '123456' //change password to your key password
        }
        configJaxspice {
            keyAlias 'jaxspice'
            keyPassword '123456' //change password to your key password
            storeFile file('./src/jaxspice/upload-keystore.jks')
            storePassword '123456' //change password to your key password
        }
        configAfgrill {
            keyAlias 'afgrill'
            keyPassword '123456' //change password to your key password
            storeFile file('./src/afgrill/upload-keystore.jks')
            storePassword '123456' //change password to your key password
        }
    }

    flavorDimensions "flavor-type"

    productFlavors {
        nopal {
            dimension "flavor-type"
            applicationId "com.nopal.app"
            resValue "string", "app_name", "Nopal Dos"
            signingConfig signingConfigs.configNopal
        }
        maa {
            dimension "flavor-type"
            applicationId "com.fasttech.maakitchen"
            resValue "string", "app_name", "Maa Kitchen"
            signingConfig signingConfigs.configMaa
        }
        santo {
            dimension "flavor-type"
            applicationId "com.fasttech.cafesantorini"
            resValue "string", "app_name", "Cafe Santorini"
        }
        hcb {
            dimension "flavor-type"
            applicationId "com.halalchinabox.app"
            resValue "string", "app_name", "Halal China Box"
        }
        apnaa {
            dimension "flavor-type"
            applicationId "com.fasttechapnabazar.app"
            resValue "string", "app_name", "Apnaa Bazar"
        }
        willyb {
            dimension "flavor-type"
            applicationId "com.willybsteakhouse.fasttech"
            resValue "string", "app_name", "Steakhouse"
        }
        chaatHouse {
            dimension "flavor-type"
            applicationId "com.chaathouse.app"
            resValue "string", "app_name", "Chaat House"
        }
        kabsah {
            dimension "flavor-type"
            applicationId "com.scalecafe.app"
            resValue "string", "app_name", "kabsah"
        }
        fifthE {
            dimension "flavor-type"
            applicationId "com.element5th.app"
            resValue "string", "app_name", "5th Element"
        }
        alladinGrill {
            dimension "flavor-type"
            applicationId "com.alladingrill.app"
            resValue "string", "app_name", "Aladdin Grill"
        }
        jaxspice {
            dimension "flavor-type"
            applicationId "com.jaxspice.com"
            resValue "string", "app_name", "jaxSpice"
        }
        afgrill {
            dimension "flavor-type"
            applicationId "com.fasttech.eatatanfgyros"
            resValue "string", "app_name", "ANF Gyros Grill"
        }
    }

    compileSdkVersion 34
    ndkVersion flutter.ndkVersion

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = '1.8'
    }

    sourceSets {
        main.java.srcDirs += 'src/main/kotlin'
    }

    defaultConfig {
        minSdkVersion 23
        targetSdkVersion 34
        versionCode flutterVersionCode.toInteger()
        versionName flutterVersionName
        multiDexEnabled true
    }

    
    buildTypes {
        release {
            crunchPngs false
            minifyEnabled true
            shrinkResources true
        }
    }
}

flutter {
    source '../..'
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk7:$kotlin_version"
    implementation "com.google.firebase:firebase-messaging:21.0.1"
}
